1) Create new branch

git branch day-2 

2) push to origin to the new branch

git push origin day-2


Switch Branch using git checkout

$ git checkout <existing_branch>

$ git checkout -b <new_branch>



First, make sure that the target branch exists by running the “git branch” command.

$ git branch


Day-2 

if you  want to center an element by position top: 50%. 
Then the top of an element could be in the middle but if you want the middle be in the middle 
set the property:
		  transform: translateY(-50%);